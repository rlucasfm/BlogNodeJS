<div class="jumbotron">
  <h1 class="display-4">Bem-vindo!</h1>
  <p class="lead">Sinta-se a vontade. Aqui eu irei mostrar as coisas mais interessantes que tiver feito...</p>
  <hr class="my-4">
  <p>Este blog foi totalmente construído com NodeJS.</p>
  {{#if user}}
  {{else}}
  <a class="btn btn-primary btn-lg" href="/usuarios/login" role="button">Acessar sua conta</a>
  <a class="btn btn-success btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
  {{/if}}
</div>
<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">        
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p> 
        <button onclick="location.href='/postagem/{{slug}}'" class="btn btn-success">Leia mais</button> 
        <hr>
        <small>{{categoria.nome}}</small><br>
        <small>Data de Publicação: {{data}}</small>      
    </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}